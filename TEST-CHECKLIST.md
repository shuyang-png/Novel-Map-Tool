# 测试功能点清单

## 快速冒烟测试

> 每次代码改动后快速验证的核心流程。

| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| SM-01 | 缩放上下限 | 滚轮放大至 200% 再缩小至 10% | 缩放比例始终在 0.1~2 之间，画布尺寸相应变化 | 高 |
| SM-02 | 基础平移 | 拖拽画布一次 | 画布随鼠标移动，未出现空白边缘 | 高 |
| SM-03 | 添加并保存坐标点 | 手动表单添加一点 → 保存当前地图 → 重新加载 | 点在画布位置、属性、列表全部保持一致 | 高 |
| SM-04 | 备注框弹出与保存 | 打开备注框输入 <200 字 → 保存 → 再次打开查看 | 内容完整保存，字符不超限 | 高 |
| SM-05 | 添加矩形范围标识 | 添加矩形 → 检查颜色/线型 → 编辑后保存 | 红色实线矩形出现且可编辑，保存后仍存在 | 高 |
| SM-06 | 单文件 JSON 读取 | 加载刚保存的地图文件 | 所有元素（点、标识、单位、关联）恢复，列表同步 | 高 |
| SM-07 | 清空数据后重新加载 | 清空所有数据 → 加载已保存文件 | 旧数据重新出现，功能未受清空影响 | 高 |
| SM-08 | 列表同步检查 | 添加/编辑/删除点后观察列表 | 列表实时反映当前状态，顺序正确 | 高 |
| SM-09 | 工作目录保存加载 | 选择目录 → 保存 → 刷新 → 自动恢复目录 → 加载文件 | 不弹下载框，文件直接写入和读取 | 高 |
| SM-10 | 关联双向显示 | 地图 A 关联地图 B → 切换到地图 B | 两张地图都显示 🚪 传送门标记 | 高 |
| SM-11 | 新建地图 | 点击新建地图 → 输入名称 → 添加元素 | 页面不刷新，空白画布上可正常操作 | 高 |
| SM-12 | 断网保存 | 断开网络后保存地图 | 原生 saveAs 正常触发下载，无报错 | 高 |

## 1. 缩放
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| Z-01 | 缩放范围下限 | 使用鼠标滚轮向下滚动，直至缩放比例达到最小值 | 缩放比例不低于 0.1（10%），画布尺寸相应缩小 | 高 |
| Z-02 | 缩放范围上限 | 使用鼠标滚轮向上滚动，直至缩放比例达到最大值 | 缩放比例不超过 2（200%），画布尺寸相应放大 | 高 |
| Z-03 | 连续缩放平滑度 | 连续快速滚动滚轮 5 次 | 缩放动画流畅，延迟 ≤ 0.5 秒，无卡顿 | 中 |
| Z-04 | 缩放后坐标准确性 | 缩放后在已标记点上单击，显示该点坐标 | 显示的坐标与原始数据保持一致（比例换算正确） | 高 |
| Z-05 | 缩放后 UI 元素自适应 | 缩放后检查按钮、列表等 UI 是否仍可点击且不被遮挡 | 所有交互控件可正常使用，未出现重叠 | 中 |

## 2. 平移
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| P-01 | 拖拽平移基本功能 | 按住鼠标左键并拖动画布 | 画布随鼠标移动平移，位置实时更新 | 高 |
| P-02 | 边缘限制 | 将画布拖至左/上/右/下边缘 | 拖拽至边缘后画布不再继续移动，防止出现空白区域 | 高 |
| P-03 | 拖拽与缩放交互 | 先缩放到 150%，再进行拖拽 | 拖拽仍然平滑且受缩放影响的移动距离正确 | 中 |
| P-04 | 连续拖拽响应 | 长时间连续拖拽 10 秒以上 | 无卡顿，画布位置持续更新 | 中 |

## 3. 坐标点
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| M-01 | 点击添加坐标点 | 在画布任意位置单击 | 弹出标记编辑框，默认填入坐标，点成功添加至列表 | 高 |
| M-02 | 手动表单添加 | 在坐标点表单中填入名称、X、Y、备注，点击"添加坐标点"按钮 | 新点出现在画布对应位置，列表显示完整信息 | 高 |
| M-03 | 查看坐标点信息 | 单击已存在的坐标点 | 弹出显示该点名称、坐标、备注的详情框 | 高 |
| M-04 | 编辑坐标点 | 在列表或弹框中修改名称或备注，保存 | 点的属性更新，画布标记文字同步变化 | 中 |
| M-05 | 删除坐标点 | 选中列表中的点，点击删除 | 点从画布及列表中消失，相关数据从内部结构中移除 | 高 |
| M-06 | 坐标点持久化 | 添加点后保存当前地图 JSON，刷新页面后加载 | 点信息完整恢复，位置、名称、备注均保持一致 | 高 |

## 4. 备注框
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| N-01 | 触发备注框 | 通过 showNoteBox（右键或对应按钮）打开备注弹窗 | 弹窗出现，输入框限制为 200 字 | 高 |
| N-02 | 超长输入限制 | 在备注框输入超过 200 字的内容并尝试保存 | 超出部分被截断或阻止保存，提示字符上限 | 高 |
| N-03 | 编辑已保存备注 | 打开已有备注进行编辑并保存 | 修改后内容立即在 UI 中更新，数据持久化 | 中 |
| N-04 | 删除备注 | 在弹窗中点击删除或在列表中删除 | 备注从地图上消失，关联数据被清除 | 高 |
| N-05 | 备注与坐标点关联 | 为特定坐标点添加备注后切换点显示 | 该点悬停或点击时能够显示对应备注 | 中 |

## 5. 范围标识
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| R-01 | 添加矩形标识 | 选择"矩形"，输入四顶点坐标，点击"添加标识" | 画布上出现红色实线矩形，列表中显示名称与坐标 | 高 |
| R-02 | 添加圆形标识 | 选择"圆形"，输入圆心坐标和半径，点击"添加标识" | 画布上出现红色实线圆形，列表中显示名称与参数 | 高 |
| R-03 | 编辑矩形标识 | 选中列表中的矩形，修改任意顶点坐标并保存 | 矩形形状实时更新，数据同步到内部结构 | 中 |
| R-04 | 编辑圆形标识 | 修改圆心或半径后保存 | 圆形尺寸/位置更新，列表信息同步 | 中 |
| R-05 | 删除范围标识 | 在列表中删除任意标识 | 对应图形从画布消失，内部数据被清除 | 高 |
| R-06 | 标识样式固定 | 检查绘制的矩形/圆形颜色与线型 | 均为红色实线，无其它样式选项 | 高 |

## 6. 量化单位
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| U-01 | 添加自定义单位 | 在单位输入框填写名称和说明，点击保存 | 单位名称显示在右下角，说明可在 tooltip 中查看 | 高 |
| U-02 | 单位持久化 | 保存地图后刷新页面再加载 | 单位名称与说明保持不变，仍显示在右下角 | 高 |
| U-03 | 编辑单位 | 修改已有单位的说明并保存 | 更新后的说明立即在 UI 中生效 | 中 |
| U-04 | 删除单位 | 删除已添加的单位 | 右下角不再显示该单位，相关数据被清除 | 中 |

## 7. 地图关联
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| L-01 | 添加双向关联 | 填写当前地图、目标地图及两侧坐标，点击"添加关联" | 关联列表出现，画布上以蓝色箭头+文字标示关联 | 高 |
| L-02 | 双向显示验证 | 打开目标地图，检查是否显示回指的关联箭头 | 两个地图之间均出现对应的箭头文字 | 高 |
| L-03 | 编辑关联坐标 | 修改任一侧坐标后保存 | 关联箭头位置实时更新，文字保持对应地图名称 | 中 |
| L-04 | 删除关联 | 在关联列表中删除一条关联 | 关联箭头从两张地图上消失，内部关联数据被清除 | 高 |

## 8. JSON 保存
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| J-01 | 保存当前地图 (saveCurrentMap) | 点击"保存当前地图"按钮 | 生成 JSON 文件，内容包含地图尺寸、坐标点、范围标识、单位、关联等全部数据 | 高 |
| J-02 | 保存元素到文件 (saveElementsToFile) | 点击对应按钮 | 仅导出当前地图的元素（坐标点、标识等）JSON，结构符合预期 | 中 |
| J-03 | 导出所有地图 (exportAllMaps) | 点击"导出所有地图" | 生成包含所有已保存地图的 JSON，完整度与单图保存一致 | 中 |
| J-04 | 保存后文件完整性校验 | 对生成的 JSON 使用 JSON lint 检查 | 文件通过语法校验，无缺失字段 | 高 |
| J-05 | 保存后重新加载验证 | 保存后立即使用加载功能重新打开 | 所有数据（点、标识、单位、关联等）保持 1:1 还原 | 高 |

## 9. JSON 读取
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| Ld-01 | 单文件加载 (loadMap) | 选择已保存的单地图 JSON，点击加载 | 画布呈现对应地图，所有元素恢复 | 高 |
| Ld-02 | 批量加载 (batchLoadMaps) | 选择多个地图文件，点击批量导入 | 所有地图依次加入已保存列表，可切换查看 | 中 |
| Ld-03 | 加载异常文件 | 用格式错误的 JSON 文件尝试加载 | 系统给出错误提示，未导致崩溃或数据污染 | 高 |
| Ld-04 | 加载后列表同步 | 加载完成后检查已保存地图列表 | 新加载的地图出现在列表并可选中 | 高 |

## 10. 数据清空
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| C-01 | 清空全部数据 (clearAllData) | 点击"清空所有数据"按钮并确认 | 画布、列表、单位、关联等全部被清空，内部结构重新初始化 | 高 |
| C-02 | 清空后保存验证 | 清空后立即执行保存操作 | 生成的 JSON 为空或仅包含默认结构，不包含旧数据 | 中 |
| C-03 | 清空后重新加载旧文件 | 清空后尝试加载之前保存的文件 | 文件能够正常恢复数据，清空不影响加载功能 | 高 |

## 11. 列表同步
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| S-01 | 添加元素后列表即时刷新 | 添加坐标点、范围标识、单位或关联后观察对应列表 | 列表立即显示新增项，顺序与添加顺序一致 | 高 |
| S-02 | 删除/编辑后列表同步 | 删除或编辑任一元素 | 列表实时更新，删除项消失，编辑信息更新 | 高 |
| S-03 | 切换地图后列表切换 | 使用 switchMap 切换到另一已保存地图 | 所有列表（点、标识、单位、关联）切换为目标地图的内容 | 高 |
| S-04 | 批量导入后列表刷新 | 批量导入多个地图后检查列表 | 已导入的每个地图出现在已保存地图列表中 | 中 |

## 12. 工作目录（File System Access API）
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| F-01 | 选择工作目录 | 点击"选择工作目录"按钮，选择一个本地文件夹 | 状态显示变为"工作目录：xxx"，底部出现该目录的 .json 文件列表 | 高 |
| F-02 | 目录句柄持久化 | 选择目录后刷新页面 | 自动恢复工作目录状态，无需重新选择 | 高 |
| F-03 | 直接写入目录 | 选择目录后点击"保存地图" | 文件直接写入目录（不弹下载框），目录文件列表更新 | 高 |
| F-04 | 从目录加载 | 点击目录文件列表中的"加载"按钮 | 地图数据从目录文件直接加载，画布和列表完整恢复 | 高 |
| F-05 | 降级到下载模式 | 不选择工作目录，点击"保存地图" | 使用原生 saveAs 弹出浏览器下载框，无 CDN 依赖 | 高 |
| F-06 | 不支持浏览器 | 在 Firefox/Safari 中打开 | 显示"浏览器不支持"提示，所有功能降级到原有方式 | 中 |
| F-07 | 切换工作目录 | 已有目录时再次点击"选择工作目录"选择新目录 | 弹出确认提示，确认后清空当前数据，状态更新为新目录，文件列表切换 | 高 |

## 13. 传送门关联图标
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| TP-01 | 传送门图标显示 | 添加一条关联，查看地图 | 关联位置显示 🚪 emoji + 目标地图名称 | 高 |
| TP-02 | 缩放后图标等比例 | 缩放到 50% 和 200% 查看传送门 | 🚪 和文字大小随缩放等比例变化 | 高 |

## 14. 双向关联 JSON 双写
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| B-01 | 添加关联双写 | 在地图 A 添加关联到地图 B（有工作目录） | 地图 A 的 JSON 和地图 B 的 JSON 都更新了关联信息 | 高 |
| B-02 | 双向显示 | 切换到地图 B 查看 | 地图 B 上显示 🚪 + 地图 A 名称（反向关联） | 高 |
| B-03 | 删除关联双写 | 删除地图 A 上的关联 | 地图 B 的 JSON 中对应反向关联也被删除 | 高 |
| B-04 | 无目录时关联 | 不选工作目录，添加关联 | savedMaps 内存中双向关联正确，切换地图后可见 | 中 |

## 15. 新建地图
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| NM-01 | 基础新建地图 | 点击"新建地图"按钮 | 页面不刷新，画布清空，所有列表清空，mapName 重置为空 | 高 |
| NM-02 | 未保存提示 | 有未保存修改时点击新建 | 弹出确认提示，询问是否保存当前修改 | 高 |
| NM-03 | 保存后新建 | 先保存当前地图 → 点击新建 → 输入新名称 → 添加元素 → 保存 | 新地图保存为独立文件，旧地图数据不受影响 | 高 |
| NM-04 | 新建后缩放重置 | 点击新建 | 缩放比例重置为 1（100%），画布位置回到原点 | 中 |

## 17. 地图关联坐标去重检查
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| DD-01 | 源坐标重复检查 | 同一源坐标 (100,900) 添加两条关联到不同目标 | 第二条关联被阻止，提示"该坐标已存在关联" | 高 |
| DD-02 | 目标坐标重复检查 | 不同源坐标指向同一目标坐标 | 提示"目标坐标已被占用"，阻止创建 | 高 |
| DD-03 | 不同目标允许 | 同一源坐标指向不同目标的不同坐标 | 允许创建（多出口规则） | 高 |
| DD-04 | 删除后可复用 | 删除已有关联后，使用相同坐标添加新关联 | 允许创建，无重复警告 | 中 |
| DD-05 | 提示信息准确 | 触发去重检查时 | 提示信息包含冲突坐标 (x,y) 和目标地图名称 | 中 |

## 18. 切换工作目录时清空旧数据
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| CD-01 | 切换目录确认弹窗 | 已选目录时点击"选择工作目录"选择新目录 | 弹出确认框"切换工作目录将清空当前未保存的数据，是否继续？" | 高 |
| CD-02 | 确认后清空数据 | 点击确认切换 | 画布清空，所有列表清空，状态显示更新为新目录 | 高 |
| CD-03 | 取消切换 | 点击取消 | 当前数据和目录保持不变 | 中 |
| CD-04 | 新目录文件列表 | 确认切换到新目录 | 底部自动列出新目录下的 .json 文件列表 | 高 |
| CD-05 | 加载新目录文件 | 切换目录后点击文件列表中的加载 | 地图数据从新目录正确加载 | 高 |

## 19. 完全离线化
| 编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|------|--------|----------|----------|--------|
| OF-01 | 断网可用性 | 断开网络后打开 HTML 文件 | 所有功能正常，无网络请求错误 | 高 |
| OF-02 | 保存下载功能 | 断网状态下点击"保存当前地图"（无工作目录） | 使用原生 saveAs 触发浏览器下载，不依赖 FileSaver.js | 高 |
| OF-03 | 无外部脚本引用 | 检查 HTML `<head>` 部分 | 无 CDN script 引用，无 FileSaver.js 等外部依赖 | 高 |
| OF-04 | 原生 saveAs 正确性 | 保存 JSON 后打开下载的文件 | 文件格式正确，内容完整，可正常加载回显 | 高 |

---
